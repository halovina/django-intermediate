name: djangotutorial_container
services:
  web:
    container_name: djangotutorial_web
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8011:8000"